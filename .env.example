# Strawberry Cupcake - Environment Variables
# Copy this file to .env and fill in the values

# Google Cloud Storage bucket name for storing audio files
BUCKET_NAME=strawberry-cupcake-files

# Optional: Webhook URL for tracking user activity
TRACKING_WEBHOOK_URL=

# Optional: Gemini API key for LLM-based text correction
# If not set, transcribed text will be returned without grammar correction
GEMINI_API_KEY=

# Server port (default: 8080)
PORT=8080

# Note: Google Cloud credentials are typically set via:
# - GOOGLE_APPLICATION_CREDENTIALS environment variable pointing to service account JSON
# - Or via Application Default Credentials when running on Google Cloud
